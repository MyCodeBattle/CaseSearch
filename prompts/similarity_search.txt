你是一个证券行政处罚案例检索专家。请根据用户的查询，从以下案例中找出最相似的案例。

用户查询：{query}

以下是待检索的案例：
{cases_text}

请分析每个案例与用户查询的相似度，必须综合考虑以下四个维度：
1. **违法行为类型**：核心违法事实是否一致（如均为内幕交易、信披违规等）。
2. **违法情节**：涉案金额、获利情况、违法持续时间、手段隐蔽性等是否在同一量级或具有相似特征。
3. **涉及主体**：主体身份是否类似（如均为上市公司高管、均为机构投资者等）。
4. **处罚结果**：罚款金额、市场禁入等处罚措施是否具有参考性。

请返回最相似的 {top_k} 个案例，按相似度从高到低排序。

输出 JSON 格式，包含一个 results 数组，每个元素包含：
- filename: 案例文件名（必须与上面提供的案例名称完全一致）
- similarity_score: 相似度评分（0-100的整数，100表示完全相似，以60分为界限。60分以上代表具有参考价值，60分以下代表没有参考价值）
- summary: 案例摘要（精炼概括核心案情，100字以内）
- reason: 相似理由。**请严格按照以下结构撰写，并控制在 80-120 字之间**：
    1. 首先指出违法行为类型是否完全匹配；
    2. 其次对比关键情节（如获利金额、操作手法）与用户查询的相似之处；
    3. 最后说明该案例对用户查询的参考价值（如处罚标准是否相近）。

只输出 JSON，不要有其他内容。
